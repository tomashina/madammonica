{{ header }}

<!-- Glami piXel for KOZO.hr -->
<script>
(function(f, a, s, h, i, o, n) {f['GlamiTrackerObject'] = i; f[i]=f[i]||function(){(f[i].q=f[i].q||[]).push(arguments)};o=a.createElement(s), n=a.getElementsByTagName(s)[0];o.async=1;o.src=h;n.parentNode.insertBefore(o,n) })(window, document, 'script', '//www.glami.hr/js/compiled/pt.js', 'glami');
glami('create', '78FEF97D04F20D6F870D45C4534F59BF', 'hr');
glami('track', 'PageView');
glami('track', 'Purchase', {
item_ids: [{% for prod in order_products %}'{{ prod.model }}{% if prod.value %}.{{ prod.value }}{% endif %}',{% endfor %}], // bought product IDs. Use the same IDs as you use in the feed 
value: {{ total }}, // order value (sum of product values)
currency: 'HRK', // order value currency
transaction_id: '{{ order_id }}' // order ID
});
</script>
<!-- End Glami piXel -->
<div class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-md-9 col-sm-8' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% endif %}
    <div id="content" class="{{ class }} mb-50">{{ content_top }}
      <h1 id="page-title">{{ heading_title }}</h1>
      <div class="inline-link">{{ text_message }}</div>
      {{ content_bottom }}</div>
    {{ column_right }}</div>
</div>
{{ footer }}